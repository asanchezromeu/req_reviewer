# Empty (same-origin) when FastAPI serves this build directly; set to the
# backend's origin (e.g. http://localhost:8000) when running `npm start` in dev.
REACT_APP_BACKEND_URL=

# Must match one of the backend's comma-separated API_KEYS. Leave both unset
# for local development with backend auth disabled.
REACT_APP_API_KEY=
